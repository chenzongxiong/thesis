\documentclass{standalone}
\usepackage{tikz}
\usepackage{amsmath}
\usepackage{pgfplots}

\begin{document}
\begin{tikzpicture}[font=\small]
    % \draw[->] (-3.5,0) -- (3.5,0) node[right] {$x$};
    % \draw[->] (0,-1.2) -- (0,1.2) node[above] {$f(x)$};
    % \draw[scale=1,domain=-3:3,smooth,variable=\x,blue] plot ({\x},{(exp(\x)-exp(-\x))/(exp(\x)+exp(-\x))});
    
    
    %\draw[scale=1,domain=-3:0,smooth,variable=\x,blue] plot ({\x},{exp(\x)-1});
    % \draw[dashed, scale=1,domain=-3.2:3.2,smooth,variable=\x,blue] plot ({\x},{-1});
    
        \begin{axis}[
            unit vector ratio*=1 1 1,
            legend pos=north west,
            xmin=-3.5, xmax=3.5, ymin=-1.5, ymax=1.5, grid=off,
            ylabel={$R(z)$}, xlabel={$z$}, grid=both,
            xtick={-3.5,-3.0,-2.5,-2,-1.5,...,2,2.5,3,3.5}, ytick={-1.5,-1.0,...,1.5},
            xticklabel style={font=\tiny, xshift=0.5ex},
            yticklabel style={font=\tiny, yshift=0.5ex},
            axis line style={->},
            axis x line=middle,
            axis y line=middle,
        ]
        \addplot+[mark=none, color=blue, domain=-3.5:3.5] {(exp(\x)-exp(-\x))/(exp(\x)+exp(-\x))};
        \addplot+[dashed,mark=none, color=red, domain=-3.5:3.5] {+1};
        \addplot+[dashed,mark=none, color=red, domain=-3.5:3.5] {-1};

        \end{axis}
        
\end{tikzpicture}
\end{document}

